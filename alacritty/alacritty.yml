# Window settings
window:
  dimensions:
    columns: 120
    lines: 36
  
  class:
    instance: Alacritty

# Font settings  
font:
  size: 14
  
  normal:
    family: MesloLGS NF

# Shell
shell:
  program: /bin/zsh
  
  args:
    - -l
    - -c 
    - "tmux attach || tmux"
